/**
 * First we will load all of this project's JavaScript dependencies which
 * includes Vue and other libraries. It is a great starting point when
 * building robust, powerful web applications using Vue and Laravel.
 */

require('./bootstrap');

// require('dropzone');
// window.Vue = require('vue');

// import Vue from 'vue';
// import VueRouter from 'vue-router';

// Vue.use(VueRouter);

// import Create from './create';
// import App from './myApp';

// const router = new VueRouter({
//     mode: 'history',
//     routes: [
//         {
//             path: '/create',
//             name: 'create',
//             component: require('./create.vue').default
//         },
//         {
//             path: '/',
//             name: 'home',
//             component: require('./app.vue').default
//         },
//     ],
// });

/**
 * The following block of code may be used to automatically register your
 * Vue components. It will recursively scan this directory for the Vue
 * components and automatically register them with their "basename".
 *
 * Eg. ./components/ExampleComponent.vue -> <example-component></example-component>
 */

// const files = require.context('./', true, /\.vue$/i)
// files.keys().map(key => Vue.component(key.split('/').pop().split('.')[0], files(key).default))

// Vue.component('example-component', require('./components/ExampleComponent.vue').default);
// Vue.component('my-input', require('./components/MyInput.vue').default);
// Vue.component('my-textarea', require('./components/MyTextarea.vue').default);
// Vue.component('media-input', require('./components/MediaInput.vue').default);
// Vue.component('create', require('./create.vue').default);

/**
 * Next, we will create a fresh Vue application instance and attach it to
 * the page. Then, you may begin adding components to this application
 * or customize the JavaScript scaffolding to fit your unique needs.
 */

// const app = new Vue({
//     el: '#app',
// });
